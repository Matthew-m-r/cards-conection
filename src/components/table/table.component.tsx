import "./table.styles.scss";
import { cards } from "../../mockups/cards.data";

const Table = () => {
    return (
        <div className="main-table-container">
            <h1>Hello from the table!</h1>
            {/* {cards?.map()} */}
        </div>
    );
};

export default Table;